{
  "name": "zyx-rest-starter",
  "version": "0.20.0",
  "description": "A REST API starter built with zyx packages",
  "main": "index.js",
  "scripts": {
    "dev": "NODE_ENV=development nodemon index.js",
    "debug": "NODE_ENV=debug nodemon index.js",
    "encrypt": "./_encrypt_all.sh",
    "test": "NODE_ENV=test node --test ./test",
    "tests": "NODE_ENV=test node --test ./tests",
    "test:server": "NODE_ENV=test nodemon --watch ../rest --watch ../schema --watch index.js --ext js,json index.js"
  },
  "keywords": [
    "rest",
    "starter",
    "zyx"
  ],
  "author": "Mike Terry",
  "license": "MIT",
  "dependencies": {
    "zyx-base": "^0.20.0",
    "zyx-env": "^0.20.0",
    "zyx-mongodb": "^0.20.0",
    "zyx-nodemailer": "^0.20.0",
    "zyx-rest": "^0.20.0",
    "zyx-schema": "^0.20.2",
    "zyx-system": "^0.20.0"
  },
  "devDependencies": {
    "zyx-test-api": "^0.20.1"
  }
}
